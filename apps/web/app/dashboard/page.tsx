import { redirect } from "next/navigation";

export default function DashboardPage() {
  // TODO: Replace with actual workspace selection or redirect to user's default workspace
  redirect("/dashboard/1");
}
